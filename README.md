# crypto-tax-assistant

Сервис исторических курсов ETH/USD находится в [coingecko-rate-service](./coingecko-rate-service).

Запуск через Docker Compose:

```bash
docker compose up --build coingecko-rate-service
```
